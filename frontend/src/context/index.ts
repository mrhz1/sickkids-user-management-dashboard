/**
 * Context index - exports all context providers and hooks
 */

export { AuthProvider, useAuth } from './AuthContext';
export type { AuthContextType, AuthUser, RegisterData } from './AuthContext';
